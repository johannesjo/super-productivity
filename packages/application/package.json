{
  "name": "@noura/application",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "build": "tsc -p tsconfig.json", "check": "tsc -p tsconfig.json --noEmit", "test": "vitest run" },
  "dependencies": { "@noura/domain": "workspace:*", "@sp/sync-core": "workspace:*" },
  "devDependencies": { "typescript": "^6.0.3", "vitest": "^4.1.8" }
}
