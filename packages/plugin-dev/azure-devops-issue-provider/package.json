{
  "name": "azure-devops-issue-provider",
  "version": "1.0.0",
  "description": "Azure DevOps issue provider plugin",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.8.3",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "@super-productivity/plugin-api": "file:../../plugin-api"
  }
}
