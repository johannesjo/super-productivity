import { LS_PREFIX } from '../app.constants';

export const TASK_FEATURE_NAME = 'tasks';

// local storage ids
export const LS_TASKS = LS_PREFIX + 'TASKS';
export const LS_CURRENT_TASK_ID = LS_PREFIX + 'CURRENT_TASK_ID';