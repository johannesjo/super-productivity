export enum MODEL_VERSION {
  TASK = 3.3337,
  // needs to be always the same as TASK !!!
  TASK_ARCHIVE = 3.3337,
  PROJECT = 6.0,
  GLOBAL_CONFIG = 2.2006,
  METRIC = 1.0,
  SIMPLE_COUNTER = 2.0,
  NOTE = 1.0,
  TAG = 1.0,
  TASK_REPEAT = 1.3,
}
