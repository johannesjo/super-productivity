// import { initialNoteState, noteReducer } from './note.reducer';
// import { moveNoteToOtherProject } from './note.actions';
//
// describe('Note Reducer', () => {
//   describe('unknown action', () => {
//     it('should return the initial state', () => {
//       const action = {} as any;
//       const result = noteReducer(initialNoteState, action);
//       expect(result).toBe(initialNoteState);
//     });
//
//     it('should update the note projectId', () => {
//       const action = moveNoteToOtherProject();
//       const result = noteReducer({
//         ids: ['NOTE_ID'],
//         entities: {
//
//         }
//       }, action);
//       expect(result).toBe(initialNoteState);
//     });
//   });
// });
