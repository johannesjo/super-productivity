// import { PFAPICfg } from '../pfapi.model';
//
// export const getFilePathFromModelId = (modelId: string, cfg: PFAPICfg): string => {
//   const modelCfg = cfg.modelCfgs.find((modelCfg) => modelCfg.modelId === modelId);
//   if (!modelCfg) {
//     throw new Error(`Model with id ${modelId} not found`);
//   }
//
//   return modelCfg.filePath;
// };
