# Local Docker stack. Use unique production secrets.
POSTGRES_PASSWORD=change-me
JWT_SECRET=replace-with-at-least-32-random-characters
CORS_ORIGINS=http://localhost:8080,http://localhost:5173

# Destructive test endpoints remain disabled unless both values are set.
TEST_MODE=false
TEST_MODE_CONFIRM=
