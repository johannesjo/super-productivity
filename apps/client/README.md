# Noura client

The Svelte 5 web/PWA and Tauri 2 desktop client.

```sh
bun run dev
bun run check
bun run lint
bun run test
bun run tauri dev
```

Static web output is written to `build/`. Desktop configuration, capabilities, icons, Rust host code, and migrations live in `src-tauri/`.
